<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <title>Shortened Url App</title>

</head>

<body>

    <div class="container mt-5">
        <h1 class="text-center">Shortened URL App</h1>
        <div>

            <form method="POST" action="/url">
                <div class="form-floating mb-3">
                    <input type="text" class="form-control mt-2" name="url" id="floatingInput"
                        placeholder="https://example.com">
                    <label for="floatingInput">Enter Your Original Url</label>
                </div>
                <button type="submit" class="btn btn-secondary mb-2">Submit</button>
            </form>
        </div>
        <% if (locals.id) { %>
            <p>URL Generated http://localhost:8001/url/<%= id %>
            </p>
            <% } %>
                <div>
                    <% if (locals.urls) { %>
                        <table class="table">
                            <thead>
                                <th scope="col">S. No</th>
                                <th scope="col">Short Url</th>
                                <th scope="col">Redirect Url</th>
                                <th scope="col">Clicks</th>
                            </thead>
                            <tbody>
                                <% urls.forEach((url, index)=> { %>
                                    <tr>
                                        <td scope="row">
                                            <%= index + 1 %>
                                        </td>
                                        <td>
                                            <a href="/url/<%= url.shortId %>">
                                                <%= url.shortId %>
                                            </a>
                                        </td>
                                        <td>
                                            <%= url.redirectURL %>
                                        </td>
                                        <td>
                                            <%= url.visitHistory.length %>
                                        </td>
                                    </tr>
                                    <% }) %>
                            </tbody>
                        </table>
                        <% } %>
                </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>

</html>